class Solution {
public:
    int searchInsert(vector<int>& nums, int target) {
        // if target is found
     
        int i = 0;
        while(i < nums.size() && target > nums[i]){
            ++i;
        }
        return i;
        
   
    }
};
